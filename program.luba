// You can set vars
let x = 1

let myFunction = fn(name?, day = "Monday") { // Comments will be ignored
    return if name { (name + ", today is ") + day } else { myFunction("Unknown person", day) }
}

switch 16 /* even this comment is ignored */ {
    case 2 "no"
    case 16 myFunction("Herobrine", "Sunday")
}

if true {
    return a
}